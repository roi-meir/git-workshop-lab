# Setup check

Run in a terminal (Git Bash on Windows).

```bash
git --version
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
git config --global --list
```

Use the same email as your GitHub/GitLab account.
